import React from "react";
import Head from "next/head";
import InstitutionPage from "@page-components/institution";

const Institution = () => (
  <>
    <Head>
      <title>Institution</title>
    </Head>
    <InstitutionPage />
  </>
);

export default Institution;
