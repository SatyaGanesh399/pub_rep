import React from "react";
import Head from "next/head";
import EventsPage from "@page-components/events";

const Events = () => (
  <>
    <Head>
      <title>Events</title>
    </Head>
    <EventsPage />
  </>
);

export default Events;
